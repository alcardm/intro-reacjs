function Rutax({ Logo }) {
  let estiloCover = {
    border: "0.2em solid white",
    color: "var(--bs-code-color)",
  };
  return (
    <h2 style={estiloCover}>
      Esta es la ruta <i>X</i>
    </h2>
  );
}
export default Rutax;
